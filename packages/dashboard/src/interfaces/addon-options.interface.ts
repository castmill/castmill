/**
 * AddOnOptions Interface
 *
 * This interface represents the options that can be passed to an AddOn component.
 */
export interface AddOnOptions {
  /**
   * Not sure which fields we need yet, but most likely some kind of App wide object should be passed to the AddOn
   * to allow it to interact with the rest of the application.
   */
}

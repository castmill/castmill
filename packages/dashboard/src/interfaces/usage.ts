export interface Usage {
  medias: {
    total: number;
    used: number;
  };
  storage: {
    total: number;
    used: number;
  };
  users: {
    total: number;
    used: number;
  };
  devices: {
    total: number;
    used: number;
  };
  playlists: {
    total: number;
    used: number;
  };
  channels: {
    total: number;
    used: number;
  };
  teams: {
    total: number;
    used: number;
  };
}

# Castmill Widget Editor

This package is a WIP for a Widget Template editor. 


## Build Widgets

Use ```yarn build``` to build all widgets into a Json format that can be imported by castmill as built-in widgets.

